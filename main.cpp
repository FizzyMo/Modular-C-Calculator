// Includes the Calculator class definition to create and use a Calculator object
#include "calculator.h"

// Main program

int main() {
    Calculator calc;

    // Start the calculator application
    calc.run();

    // Indicate successful run of application
    return 0;
}